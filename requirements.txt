# Verticore — requirements.txt
# Version ranges matched to MSI Engine's tested, production-proven set.
# Trimmed: no supabase / spacy (MSI-specific, not used by Verticore Phase 1).

streamlit>=1.40.0,<2.0.0
pandas>=2.0.0,<4.0.0
gspread>=6.0.0,<7.0.0
google-auth>=2.0.0,<3.0.0
google-auth-oauthlib>=1.0.0,<2.0.0
requests>=2.28.0,<3.0.0
